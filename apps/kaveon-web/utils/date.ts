// Date helpers - return raw date strings from SQL without formatting
export function formatDatasetDate(value?: string | null): string {
  if (!value) return "";
  // Return raw SQL date string without any JS formatting
  return value;
}

export function formatSavedQueryDate(value: string | null): string {
  if (!value) return "";
  // Return raw SQL date string without any JS formatting
  return value;
}