[node]
environment = "docker"
coordinator = false

[http]
port = 8080

[discovery]
uri = "http://coordinator:8080"

[storage]
data_dir = "/data"
catalog_dir = "/etc/kaveon/catalogs"

[catalog]
database_path = "/tmp/kaveon/catalog.db"
